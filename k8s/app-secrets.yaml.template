# This is a template file - DO NOT commit with actual secrets
# The CD workflow will generate the actual secret from GitHub Secrets

apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  JWT_SECRET: "REPLACE_WITH_ACTUAL_SECRET"
  MYSQL_ROOT_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
